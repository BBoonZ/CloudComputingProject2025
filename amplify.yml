version: 1
frontend:
  phases:
    build:
      commands:
        - cd travel-planner
        - npm install
        - npm run build
  artifacts:
    baseDirectory: travel-planner/build
    files:
      - '**/*'
  cache:
    paths:
      - travel-planner/node_modules/**/*